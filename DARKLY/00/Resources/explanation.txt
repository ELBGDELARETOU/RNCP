explication:

les identifiants sont envoyes dans l URL en utilisant une methode GET et non POST,
tout est donc visible dans l URL,il suffit de faire un script python qui en calquant l URL
vas en boucle envoyer des requettes GET pour tester tout un tas de mots de passe depuis
rockyou qui est une base de donnees de mot de passe les plus souvent utiliser.